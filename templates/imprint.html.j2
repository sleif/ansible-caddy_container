<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ caddy_imprint.h1 }}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      padding: 20px;
    }
    h1 {
      font-size: 24px;
      margin-bottom: 20px;
    }
    h2 {
      font-size: 20px;
      margin-bottom: 10px;
    }
    p {
      margin-bottom: 10px;
    }
    address {
      font-style: normal;
    }
  </style>
</head>
<body>
  <h1>{{ caddy_imprint.h1 }}</h1>

  <p>{{ caddy_imprint.legal_address }}</p>

  <h2>{{ caddy_imprint.h2 }}</h2>
  <p>{{ caddy_imprint.contact }}</p>

</body>
</html>